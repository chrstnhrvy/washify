# `lib/` — Shared clients & utilities

Framework-agnostic helpers shared across features.

- `n8n.ts` — typed client for the live n8n webhooks (`sendSms`, `askFaq`). ✅
- **Future:** `supabase.ts` (browser client), `excel.ts` (SheetJS export),
  `format.ts` (currency/phone helpers).
