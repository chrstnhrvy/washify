# `app/` — Authenticated workspace shell

The signed-in `/app` experience (phase 2+). Holds the route shell, layout, and
protected-route guard that redirects to the landing page when there's no
Supabase session.

**Future files:** `AppShell.tsx`, `ProtectedRoute.tsx`, `router.tsx`.
