# `types/` — Shared TypeScript types

App-wide domain types mirroring the Supabase schema in `/supabase`.

**Future files:** `shop.ts`, `order.ts`, `customer.ts`, `faq.ts` (or a single
`database.ts` generated from Supabase).
