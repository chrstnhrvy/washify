# `features/dashboard/` — Sales & charts (phase 5)

Day / week / month sales views, revenue + loads charts, and one-click Excel
export (client-side via SheetJS). Follow the ui-ux-pro-max charts rules
(legends, tooltips, accessible colors, empty/loading states).

**Future files:** `SalesDashboard.tsx`, `RevenueChart.tsx`, `exportXlsx.ts`.
