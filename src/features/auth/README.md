# `features/auth/` — Google sign-in (phase 2)

Supabase Auth with the Google provider, session handling, and first-time shop
provisioning. The landing-page `onSignIn` placeholder in `App.tsx` is replaced
by the real flow here.

**Future files:** `useSession.ts`, `signInWithGoogle.ts`, `AuthProvider.tsx`.
Supabase client lives in `src/lib/supabase.ts`.
